---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: secretv1s.keymanager.openstack.m.crossplane.io
spec:
  group: keymanager.openstack.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - openstack
    kind: SecretV1
    listKind: SecretV1List
    plural: secretv1s
    singular: secretv1
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretV1 is the Schema for the SecretV1s API. Manages a V1 Barbican
          secret resource within OpenStack.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretV1Spec defines the desired state of SecretV1
            properties:
              forProvider:
                properties:
                  acl:
                    description: |-
                      Allows to control an access to a secret. Currently only the
                      read operation is supported. If not specified, the secret is accessible
                      project wide.
                    properties:
                      read:
                        properties:
                          projectAccess:
                            description: |-
                              Whether the secret is accessible project wide.
                              Defaults to true.
                            type: boolean
                          users:
                            description: |-
                              The list of user IDs, which are allowed to access the
                              secret, when project_access is set to false.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  algorithm:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  bitLength:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: number
                  expiration:
                    description: The expiration time of the secret in the RFC3339
                      timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted, a
                      secret will never expire. Changing this creates a new secret.
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Additional Metadata for the secret.
                    type: object
                    x-kubernetes-map-type: granular
                  mode:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  name:
                    description: |-
                      Human-readable name for the Secret. Does not have
                      to be unique.
                    type: string
                  payloadContentEncoding:
                    description: The encoding used for the payload to be able to include
                      it in the JSON request. Must be either base64 or binary.
                    type: string
                  payloadContentType:
                    description: The media type for the content of the payload. Must
                      be one of text/plain, text/plain;charset=utf-8, text/plain;
                      charset=utf-8, application/octet-stream, application/pkcs8.
                    type: string
                  payloadSecretRef:
                    description: The secret's data to be stored. payload_content_type
                      must also be supplied if payload is included.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  region:
                    description: |-
                      The region in which to obtain the V1 KeyManager client.
                      A KeyManager client is needed to create a secret. If omitted, the
                      region argument of the provider is used. Changing this creates a new
                      V1 secret.
                    type: string
                  secretType:
                    description: Used to indicate the type of secret being stored.
                      For more information see Secret types.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  acl:
                    description: |-
                      Allows to control an access to a secret. Currently only the
                      read operation is supported. If not specified, the secret is accessible
                      project wide.
                    properties:
                      read:
                        properties:
                          projectAccess:
                            description: |-
                              Whether the secret is accessible project wide.
                              Defaults to true.
                            type: boolean
                          users:
                            description: |-
                              The list of user IDs, which are allowed to access the
                              secret, when project_access is set to false.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  algorithm:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  bitLength:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: number
                  expiration:
                    description: The expiration time of the secret in the RFC3339
                      timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted, a
                      secret will never expire. Changing this creates a new secret.
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Additional Metadata for the secret.
                    type: object
                    x-kubernetes-map-type: granular
                  mode:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  name:
                    description: |-
                      Human-readable name for the Secret. Does not have
                      to be unique.
                    type: string
                  payloadContentEncoding:
                    description: The encoding used for the payload to be able to include
                      it in the JSON request. Must be either base64 or binary.
                    type: string
                  payloadContentType:
                    description: The media type for the content of the payload. Must
                      be one of text/plain, text/plain;charset=utf-8, text/plain;
                      charset=utf-8, application/octet-stream, application/pkcs8.
                    type: string
                  payloadSecretRef:
                    description: The secret's data to be stored. payload_content_type
                      must also be supplied if payload is included.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  region:
                    description: |-
                      The region in which to obtain the V1 KeyManager client.
                      A KeyManager client is needed to create a secret. If omitted, the
                      region argument of the provider is used. Changing this creates a new
                      V1 secret.
                    type: string
                  secretType:
                    description: Used to indicate the type of secret being stored.
                      For more information see Secret types.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: SecretV1Status defines the observed state of SecretV1.
            properties:
              atProvider:
                properties:
                  acl:
                    description: |-
                      Allows to control an access to a secret. Currently only the
                      read operation is supported. If not specified, the secret is accessible
                      project wide.
                    properties:
                      read:
                        properties:
                          createdAt:
                            description: The date the secret was created.
                            type: string
                          projectAccess:
                            description: |-
                              Whether the secret is accessible project wide.
                              Defaults to true.
                            type: boolean
                          updatedAt:
                            description: The date the secret was last updated.
                            type: string
                          users:
                            description: |-
                              The list of user IDs, which are allowed to access the
                              secret, when project_access is set to false.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: set
                        type: object
                    type: object
                  algorithm:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  allMetadata:
                    additionalProperties:
                      type: string
                    description: |-
                      The map of metadata, assigned on the secret, which has been
                      explicitly and implicitly added.
                    type: object
                    x-kubernetes-map-type: granular
                  bitLength:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: number
                  contentTypes:
                    additionalProperties:
                      type: string
                    description: The map of the content types, assigned on the secret.
                    type: object
                    x-kubernetes-map-type: granular
                  createdAt:
                    description: The date the secret was created.
                    type: string
                  creatorId:
                    description: The creator of the secret.
                    type: string
                  expiration:
                    description: The expiration time of the secret in the RFC3339
                      timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted, a
                      secret will never expire. Changing this creates a new secret.
                    type: string
                  id:
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Additional Metadata for the secret.
                    type: object
                    x-kubernetes-map-type: granular
                  mode:
                    description: Metadata provided by a user or system for informational
                      purposes.
                    type: string
                  name:
                    description: |-
                      Human-readable name for the Secret. Does not have
                      to be unique.
                    type: string
                  payloadContentEncoding:
                    description: The encoding used for the payload to be able to include
                      it in the JSON request. Must be either base64 or binary.
                    type: string
                  payloadContentType:
                    description: The media type for the content of the payload. Must
                      be one of text/plain, text/plain;charset=utf-8, text/plain;
                      charset=utf-8, application/octet-stream, application/pkcs8.
                    type: string
                  region:
                    description: |-
                      The region in which to obtain the V1 KeyManager client.
                      A KeyManager client is needed to create a secret. If omitted, the
                      region argument of the provider is used. Changing this creates a new
                      V1 secret.
                    type: string
                  secretRef:
                    description: The secret reference / where to find the secret.
                    type: string
                  secretType:
                    description: Used to indicate the type of secret being stored.
                      For more information see Secret types.
                    type: string
                  status:
                    description: The status of the secret.
                    type: string
                  updatedAt:
                    description: The date the secret was last updated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
